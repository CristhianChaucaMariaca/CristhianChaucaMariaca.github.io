include mixins/nav_in_page
include ../config/nav_in_page
.nav-in-page
    ul
        each item in navpage
            if(item.status === true)
                +nav_pages(item.text)(class="link-"+item.class) 