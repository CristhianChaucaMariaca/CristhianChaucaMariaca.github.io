mixin servicio(icon,title, description)
    article.service
        img(src="media/"+icon+".svg", alt=title, srcset="").service__icon
        h2.service__title= title
        p.service__description= description